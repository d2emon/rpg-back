/*
import express from 'express';
import request from 'request';

export const getCampaign = (req: express.Request, res: express.Response) => {
    const url = `${proxiedURL}?${querystring.stringify(req.query)}`;
    logger.info(`/fileThumbnail going to url ${url}`);
    return request
        .get(url)
        .pipe(res);
}
 */
