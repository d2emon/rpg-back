export interface Publisher {
    publisherId: number;
    name: string;
}

const publishers: Publisher[] = [
    {
        publisherId: 1,
        name: 'Publisher',
    },
]

export default publishers;
